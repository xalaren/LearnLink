export enum InputType {
    text = 'text',
    password = 'password',
}

export enum Paths {
    homePath = '/',
    loginPath = '/login',
    registerPath = '/register',
    profilePath = '/profile',
}